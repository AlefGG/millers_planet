import 'package:millers_planet/main/model/card_model.dart';

enum TimeUnit { milliseconds, seconds, minutes, hours, days }

class Constants {
  static DateTime interstellarReleaseDate = DateTime(2014, 10, 26);

  static List<CardModel> cards = [];
}
